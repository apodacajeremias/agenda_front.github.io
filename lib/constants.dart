import 'package:intl/intl.dart';

const double defaultSizing = 8.0;
const double minimumSizing = defaultSizing / 2;
const double mediumSizing = defaultSizing * 2;
const double maximumSizing = defaultSizing * 4;
final DateFormat dateFormat = DateFormat('dd/MM/yyyy');
final DateFormat timeFormat = DateFormat('HH:mm');
