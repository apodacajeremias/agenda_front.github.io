import 'package:flutter/material.dart';

class AgendaIndexView extends StatelessWidget {
  const AgendaIndexView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Placeholder(
      child: Text('AGENDA INDEX VIEW'),
    ));
  }
}
