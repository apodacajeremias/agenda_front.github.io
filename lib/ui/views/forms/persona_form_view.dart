import 'package:flutter/material.dart';

class PersonaFormView extends StatelessWidget {
  const PersonaFormView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      
    );
  }
}
